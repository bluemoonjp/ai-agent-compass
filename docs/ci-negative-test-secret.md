# Synthetic secret for gitleaks CI verification

This file exists only to verify that the `gitleaks` job fails in CI when the
synthetic pattern below is present. It is part of a throwaway test PR that
will be closed once verified (see #14).

COMPASS_TEST_SECRET_AB12CD34EF56GH78
