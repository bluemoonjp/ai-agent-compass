# This file intentionally has no frontmatter

This file exists only to verify that the `check` job fails in CI when a
practice file is missing its required frontmatter. It is part of a
throwaway test PR that will be closed once verified (see #14).
