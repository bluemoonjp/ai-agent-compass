# AGENTS.md

State this project's purpose for an agent in one line.

This file guides the agent; only a hook or a CI check your project runs actually enforces a rule.

## Map

| Path | Contents |
| --- | --- |
| `<path>` | `<what an agent needs to know lives here>` |

## Working rules

- Keep this file minimal; move anything long into a linked document. (none)
- State one canonical rule per topic; do not leave two rules that might contradict. (none)

Strip the `.template` suffix when you copy this file into your project; place it at `AGENTS.md`, not `AGENTS.md.template`.

See [adapters/README.md](../adapters/README.md) for tool-specific differences (Codex, Copilot, Gemini CLI, Cursor, ...).
